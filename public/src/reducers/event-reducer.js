const INITIAL_STATE = { loading: false,init:false, message: "", errorMessage: "", warningMessage: "" };

export default (state = INITIAL_STATE, action) =>{
  switch (action.type) {

    default:
      return state;
  }

};